<nav class="navbar">
    <div class="navbar-toggle">
        <div class="field-switch">
            <input id="pinCrateMenu" type="checkbox" name="menu-pinned" value="yes" />
            <label for="pinCrateMenu">{{ icon('pin-angle') }} Sidebar</label>
        </div>
        
        <script type="text/javascript">
            (function() {
                const menuPinned = localStorage.getItem('menu-pinned') || 'no';
                const input = document.querySelector('#pinCrateMenu');
                input.checked = menuPinned === 'yes';

                input.addEventListener('change', (event) => {
                    if (event.target.checked) {
                        localStorage.setItem('menu-pinned', 'yes');
                        document.documentElement.classList.add('menu-pinned');
                    } else {
                        localStorage.setItem('menu-pinned', 'no');
                        document.documentElement.classList.remove('menu-pinned');
                    }
                });
            })();
        </script>
    </div>

    <ul class="navbar-nav">
        <li class="nav-title">Home</li>
        <li class="nav-item active">
            <a href="" class="nav-link">
                <span class="nav-icon">{{ icon('speedometer', 20) }}</span>
                <span class="nav-text">Dashboard</span>
            </a>
        </li>
        <li class="nav-item">
            <a href="" class="nav-link">
                <span class="nav-icon">{{ icon('info-circle', 20) }}</span>
                <span class="nav-text">About</span>
            </a>
        </li>
    </ul>
        
    <ul class="navbar-nav">
        <li class="nav-title">Manage</li>
        <li class="nav-item item-emerald">
            <a href="" class="nav-link">
                <span class="nav-icon">{{ icon('collection', 20) }}</span>
                <span class="nav-text">Collections</span>
            </a>
        </li>
        <li class="nav-item item-emerald">
            <a href="" class="nav-link">
                <span class="nav-icon">{{ icon('files-alt', 20) }}</span>
                <span class="nav-text">Singletons</span>
            </a>
        </li>
        <li class="nav-item item-emerald">
            <a href="" class="nav-link">
                <span class="nav-icon">{{ icon('input-cursor', 20) }}</span>
                <span class="nav-text">Forms</span>
            </a>
        </li>
        <li class="nav-item item-emerald">
            <a href="" class="nav-link">
                <span class="nav-icon">{{ icon('tags', 20) }}</span>
                <span class="nav-text">Taxonomy</span>
            </a>
        </li>
    </ul>

    <ul class="navbar-nav">
        <li class="nav-title">Configure</li>
        <li class="nav-item item-red">
            <a href="" class="nav-link">
                <span class="nav-icon">{{ icon('people', 20) }}</span>
                <span class="nav-text">Users</span>
            </a>
        </li>
        <li class="nav-item item-red">
            <a href="" class="nav-link">
                <span class="nav-icon">{{ icon('plugin', 20) }}</span>
                <span class="nav-text">Plugins</span>
            </a>
        </li>
        <li class="nav-item item-red">
            <a href="" class="nav-link">
                <span class="nav-icon">{{ icon('palette', 20) }}</span>
                <span class="nav-text">Themes</span>
            </a>
        </li>
        <li class="nav-item item-red">
            <a href="" class="nav-link">
                <span class="nav-icon">{{ icon('wrench-adjustable-circle', 20) }}</span>
                <span class="nav-text">Settings</span>
            </a>
        </li>
    </ul>
</nav>